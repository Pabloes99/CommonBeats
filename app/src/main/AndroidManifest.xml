<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pabloes99.commonbeats">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ToolBarColor"
        >
        <activity android:name=".iu.Evento.EventoSeleccionadoActivity"></activity>
        <activity android:name=".iu.DashBoardActivity" />
        <activity android:name=".iu.SignUpActivity" />
        <activity
            android:name=".iu.LoginActivity"
            android:launchMode="singleTop" />
        <activity android:name=".iu.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>